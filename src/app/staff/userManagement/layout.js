export const metadata = {
    title: 'Roles & User Management | AMNI Contractor Portal',
  };

  const Layout = ({children}) => {
    return (
      <div>{children}</div>
    )
  }
  
  export default Layout
export const metadata = {
    title: 'Job Categories | AMNI Vendor Registration Portal',
  };

  const Layout = ({children}) => {
    return (
      <div>{children}</div>
    )
  }
  
  export default Layout
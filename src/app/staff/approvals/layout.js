export const metadata = {
    title: 'Registration Approvals | AMNI Contractor Portal',
  };

  const Layout = ({children}) => {
    return (
      <div>{children}</div>
    )
  }
  
  export default Layout
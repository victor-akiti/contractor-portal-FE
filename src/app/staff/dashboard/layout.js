export const metadata = {
    title: 'Dashboard | AMNI Vendor Registration Portal',
  };

  const Layout = ({children}) => {
    return (
      <div>{children}</div>
    )
  }
  
  export default Layout
# Contractor Portal FE — Form System & Stage Normalization

**Branch:** `claude/analyze-form-system-54EPq`
**Canonical spec:** [contractor-portal-be:SPEC.md](https://github.com/victor-akiti/contractor-portal-BE/blob/claude/analyze-form-system-54EPq/SPEC.md)

All design decisions, data model, state machine, API contracts, migration plan, and decisions-baked-in live in the BE-side `SPEC.md`. This file is a pointer and a FE-focused touch list. Do **not** make design decisions in this file — propose them as a change to the BE-side `SPEC.md` first.

---

## FE touch list

### Day 1 — Stage labelling normalization (FE half)

**New files:**
- `src/lib/stage.ts` — mirrors `getStage()`, `getSubState()`, `isInPipeline()`, `SUB_STATE_COPY`, `canonicalize()` from BE. Imported by every component that needs stage or sub-state.

**Edits:**
- `src/app/staff/approvals/ui/StageLegend.tsx` — already canonical; add a top-of-file comment `/** SOURCE OF TRUTH for the canonical stage taxonomy. See contractor-portal-be:SPEC.md §3.3. */`
- `src/app/staff/approvals/ui/FilterControls.tsx` — `approvalStages` prop passes `["A","B","C","D","E","F"]` for Pending-L2 chips; chip-to-level math = `chip.charCodeAt(0) - 65`.
- `src/app/staff/approvals/ApprovalsContainer.tsx` — replace 4 reads of `item.flags.stage` (lines 972, 1006, 1014, 1022) with `getSubState(item)` comparisons.
- `src/app/contractor/dashboard/page.tsx` — replace 2 reads at lines 450, 463 with `getSubState(company)` and `SUB_STATE_COPY` rendering.

**Deletes:**
- `src/app/staff/approvals/page-old.js` (104 KB legacy file; 4 of the 10 `flags.stage` reads die with it)

### Day 2 — `flags.*` consolidation (FE consumer side)
- Any FE filter/sort that reads `flags.approved`, `flags.stage`, `flags.level` → swap to `getStage()` / `getSubState()` from `src/lib/stage.ts`.
- TS types regenerated to drop the deprecated keys.

### Day 3 — Form-builder + renderer

**New files:**
- `src/components/form/FormRenderer.tsx` — see BE SPEC §13.1 for prop API
- `src/components/form/FormBuilder.tsx` — see BE SPEC §13.2
- `src/lib/formSchema.ts` — re-export of `shared/formSchema.ts` for FE imports
- `src/lib/fieldTypes.ts` — re-export
- `src/lib/visibility.ts` — re-export
- `src/app/staff/invite-approvals/page.tsx` — HOD invite-approval inbox (see BE SPEC §13.3)

**Edits:**
- `src/app/staff/form-builder/new/page.js` → replaced by `<FormBuilder>`
- `src/app/staff/form-builder/edit/[id]/page.js` → replaced by `<FormBuilder>`
- `src/app/contractor/application/edit/[id]/page.js` → uses `<FormRenderer mode="fill">`
- `src/app/contractor/application/view/[id]/page.js` → uses `<FormRenderer mode="view">`

**Deletes:**
- `src/app/staff/form-builder/update/[id]/` (empty skeleton)
- `src/components/approvalComponents/stageF copy/`
- 4 hardcoded `getXxxVisibility` helpers in `RegistrationFormBody.jsx` (replaced by `visibleIf` evaluator)

### Day 4 — Cutover & smoke testing
- Visual diff: 10 random submissions rendered via `<FormRenderer mode="view">` vs. current `/contractor/application/view/[id]`. Zero-diff requirement.

### Day 5 — Cleanup
- Remove all imports of `RegistrationFormBody.jsx` once form-builder owns the render path.

---

## Quick links

- [Canonical SPEC](https://github.com/victor-akiti/contractor-portal-BE/blob/claude/analyze-form-system-54EPq/SPEC.md) — every section referenced above
- BE SPEC §3 — Canonical state model (the part that touches every controller and component)
- BE SPEC §13 — FE component APIs
- BE SPEC §15 — Per-day touch list (BE side)
- BE SPEC §17 — Open audit findings (latent bugs to fix)
- BE SPEC Appendix B — Decisions baked in (read before proposing a change)

---

*Do not edit this file without a corresponding update to the BE-side SPEC.md.*
